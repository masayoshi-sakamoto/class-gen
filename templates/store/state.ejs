import { I<%= name %>Props } from '@/entities/<%= name %>'
import { IOptionsProps, EmptyOptionsPropsFactory } from '@/entities/Options'

export interface I<%= name %>State {
  byIds: {
    [id: string]: I<%= name %>Props
  }
  options: IOptionsProps | undefined
}

export const state = (): I<%= name %>State => ({
  byIds: {},
  options: EmptyOptionsPropsFactory()
})

export default state
