required:
  - id
  - title
  - order
properties:
  id:
    type: string
    example: "unique_<%= name.toLowerCase() %>_id"
  title:
    type: string
    example: "タイトル"
  order:
    type: integer
    format: int32
    example: 1
